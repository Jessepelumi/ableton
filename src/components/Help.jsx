import { useEffect } from "react";

const Help = () => {

    useEffect(() => {
        document.title = "Help and support for Live & Push | Ableton";
    }, []);

    return (
        <div className="help">
            
        </div>
    );
}
 
export default Help;